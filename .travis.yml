language: ruby
rvm:
- 2.4.1

before_install:
    - gem update --system
    - gem install bundler

script:
 — bundle install
 — bundle exec rake db:create
 — bundle exec rake db:migrate
 — bundle exec rspec